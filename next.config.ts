import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['i.ibb.co', "randomuser.me", "images.unsplash.com", "api.qrserver.com"],
  },
};

export default nextConfig;
